A variable defined using the `var` keyword cannot have a constant type.

Bad code:
```shard
var a: const i32;
//     ^^^^^ `a` is declared as a variable, but has been assigned the type of a constant.
```

Good code:
```shard
var a: i32;
// or
const a: i32;
```