Not all paths return a value.

Bad code:
```shard
fun test() : i32 {
    // the return type of the function is `i32`, but it does not return a value
}

fun main() {
    test();
}
```

Good code:
```shard
fun test() : i32 {
    return 0;
}

fun main() {
    test();
}
```