Assigning a global variable in a global scope.

Bad code:
```shard
var a: i32;
a = 20; // assigning a global variable in a global scope
```

Good code:
```shard
var a: i32 = 20;

fun main() {
    a = 10;
}
```