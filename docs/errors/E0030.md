Assigning a variable requires an expression.

Bad code:
```shard
fun main() {
    var a: i32;
    a = ;
//     ^ where is the expression?!
}
```

Good code:
```shard
fun main() {
    var a: i32;
    a = 20;
}
```